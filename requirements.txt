fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0
pydantic>=2.9.0
pymongo==4.6.0
requests==2.31.0
email-validator==2.1.0
# Upgrade SQLAlchemy to a version compatible with Python 3.13
SQLAlchemy==2.0.36
# pyodbc intentionally omitted in this environment to avoid build failures.
# The backend degrades gracefully when ODBC/pyodbc is unavailable.
python-multipart==0.0.9
passlib[bcrypt]==1.7.4
PyJWT==2.9.0
aiofiles==23.2.1
